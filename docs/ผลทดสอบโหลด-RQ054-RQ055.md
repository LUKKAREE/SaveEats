# ผลทดสอบโหลด SaveEats (RQ-054 และ RQ-055)

> สร้างอัตโนมัติโดย `backend/tools/loadTest.ts` เมื่อ 09/09/2569 18:01
> รันซ้ำได้ด้วย `npm run loadtest -- --url http://localhost:3000 --users 50 --seconds 30`

## เงื่อนไขการทดสอบ

| หัวข้อ | ค่า |
|---|---|
| ปลายทาง | `http://localhost:3000` |
| ผู้ใช้จำลองพร้อมกัน | 50 คน |
| ระยะเวลา | 31.1 วินาที |
| คำขอทั้งหมด | 1297 ครั้ง |
| อัตราคำขอ | 41.7 ครั้ง/วินาที |
| คำขอที่ล้มเหลว | 0 ครั้ง |

## ผลแยกตามเส้นทาง (หน่วยเป็นมิลลิวินาที)

| เส้นทาง | จำนวน | ล้มเหลว | เฉลี่ย | p50 | p90 | p95 | สูงสุด |
|---|---|---|---|---|---|---|---|
| หน้ารายการอาหาร (feed) | 326 | 0 | 1356 | 1261 | 1588 | 1824 | 2888 |
| ค้นหาอาหาร | 326 | 0 | 1346 | 1261 | 1557 | 1740 | 2890 |
| รายชื่อร้าน | 324 | 0 | 1341 | 1245 | 1574 | 1718 | 3010 |
| ตรวจสุขภาพฐานข้อมูล | 321 | 0 | 685 | 617 | 822 | 1019 | 2000 |
| **ทุกเส้นทางรวมกัน** | **1297** | **0** | **1184** | **1224** | **1535** | **1694** | **3010** |

## สรุปเทียบกับข้อกำหนด

| ข้อ | เกณฑ์ | ผลที่วัดได้ | สรุป |
|---|---|---|---|
| RQ-055 | รองรับผู้ใช้พร้อมกันอย่างน้อย 50 คน | ยิงพร้อมกัน 50 คน ล้มเหลว 0 ครั้ง | ผ่าน |
| RQ-054 | API ตอบกลับภายใน 2 วินาที | p95 = 1694 ms | ผ่าน |

## เรื่องที่ต้องอ่านประกอบ ห้ามตัดทิ้ง

**คำขอแรกหลังเครื่องพัก (cold start) ใช้เวลา 309 ms** และไม่ได้ถูกนับรวมในตารางข้างบน

Render แผนฟรีจะพักเครื่องเมื่อไม่มีคนใช้งานสักพัก คำขอแรกหลังตื่นจึงช้ากว่าปกติมาก
ตัวเลขนี้แยกออกมาเพราะไม่ใช่ "สภาวะการใช้งานปกติ" ตามที่ RQ-054 เขียนไว้
แต่ก็ต้องรายงานไว้ เพราะผู้ใช้คนแรกของวันเจอสถานการณ์นี้จริง

**สิ่งที่การทดสอบนี้วัด และไม่ได้วัด**

- วัด : เส้นทางอ่านข้อมูลที่ไม่ต้องล็อกอิน ซึ่งเป็นเส้นทางที่ RQ-054 ระบุให้วัด
- ไม่ได้วัด : การเขียนข้อมูล เช่น การจองพร้อมกัน ซึ่งมีชุดทดสอบแยกอยู่แล้ว (SIT-015)
- ผู้ใช้จำลองยิงคำขอถัดไปทันทีที่ได้คำตอบ ซึ่งหนักกว่าผู้ใช้จริงที่มีเวลาอ่านหน้าจอ
  ตัวเลขที่ได้จึงเป็นด้านที่แย่กว่าความจริง ไม่ใช่ด้านที่สวยกว่า
